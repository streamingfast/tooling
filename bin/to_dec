#!/bin/bash

value=""

if [ -t 0 ]; then 
    value=$1
else
    value=`cat /dev/stdin`
fi

if [[ $value == 0x* ]]; then
  value=${value:2}
fi

value=`echo $value | awk '{print toupper($0)}'`

echo "ibase=16; ${value}" | bc
